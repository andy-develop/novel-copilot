---
name: novel-copilot
description: "长篇小说全自动写作助手 v5.0：给一个开头自动成书。发散→评分→选择→写作→校验，循环不停。23大模块 + 8维评分（新颖度20%+读者惊讶度10%）+ 记忆锚点污染 + D15反常配角驱动 + 卷终反转引擎 + 终端直写技巧 + 禁用技术向隐喻 + 双重写入策略。30万字不崩盘，每章都有爽点和画面。"
version: 4.2.0
author: Hermes Agent
tags: [小说, 写作, 长篇, 逻辑一致性, 剧情发散, 角色管理, 世界观, 悬念, 翻页驱动, 叙事纪律, 读者情绪, 爽度, 画面感, 快节奏, 散文质量, 章节解剖, 伏笔发现, 质量评分, 角色深度, 情感曲线, 对话潜台词, 章节变速, 智能缓存]
trigger: 当用户要写小说、续写章节、检查设定矛盾、探索剧情分支时触发
related_skills: []
---

# 长篇小说写作助手 (Novel Copilot) v4.0

超长篇小说（30万字+）的核心难题：**写到后面忘了前面**。本 skill 通过结构化状态追踪 + 逻辑校验 + 剧情发散 + **全自动写作引擎** + **叙事纪律引擎** + **v4.0 智能增强引擎**（角色深度/伏笔发现/质量评分/章节解剖/情感编排/对话潜台词/章节变速/效率优化），确保长篇不崩盘、不注水、不让读者弃书。

## 核心理念

> 写作是发散的，校验是收敛的。先发散，再校验，最后落笔。

**叙事三优先级**（冲突时高优先级不可妥协）：
- **P0 = 节奏 + 爽度 + 画面感** — 读者留不留得住、爽不爽、记不记得住，全靠这三条
- **P1 = 一致性 + 散文质量** — 不崩盘、不啰嗦
- **P2 = 大纲 + 字数 + 状态文件** — 工具而非枷锁

> 当 P0 与 P2 冲突时（如大纲要求铺垫但铺垫会拖慢节奏），**节奏爽感优先**，调整大纲而非牺牲节奏。
## 项目结构

> **每个小说项目 = 一个 GitHub 仓库**（默认 `andy-develop/书名`，private）。初始化时自动创建，每章写完自动 commit & push。

```
my-novel/                        # ← GitHub 仓库 andy-develop/my-novel
├── .git/                        # Git 版本控制
├── state/
│   ├── characters.yaml          # 角色状态库
│   ├── world.yaml               # 世界设定库
│   ├── threads.yaml             # 剧情线索库
│   ├── timeline.yaml            # 时间线
│   ├── rules.yaml               # 世界规则（不可违反的设定）
│   ├── arc-tracker.yaml         # 弧线追踪
│   ├── emotional-debts.yaml     # 情感债追踪
│   └── consistency-log.yaml     # 一致性校验日志
├── chapters/                    # 章节正文 ch{NNN}.md
├── outlines/                    # 弧线大纲 + 章节规划
├── automation-config.yaml       # 自动写作配置
└── novel-project.yaml           # 项目元信息


## 二十三模块概览

| 模块 | 名称 | 优先级 | 核心功能 |
|------|------|--------|---------|
| 一～五 | 状态追踪层 | P1-P2 | 角色/世界/线索/时间线/规则 |
| 六 | 剧情发散引擎 | P0 | 15种模式+发散去重+记忆锚点污染+D15反常配角驱动+格局升级 |
| 七 | 自动写作引擎 | P0 | 循环/评分/门控/冲刺/卷过渡 |
| 八 | 开篇冲击力 | P0 | 第1-2章专用引擎 |
| 九 | 叙事纪律+情绪管理 | P0 | 300字入局/断章/前10章保护 |
| 十 | 爽度+画面感引擎 | P0 | 六大爽型/六技法/快节奏四原则 |
| **十一** | **散文质量层** 🆕 | P1 | 五感平衡/句式/SvT/对话/修辞 |
| **十二** | **章节解剖引擎** 🆕 | P0 | 8元素/6扩容/密度规则 |
| **十三** | **伏笔自动发现** 🆕 | P0 | 扫描→入库→回旋镖追踪 |
| **十四** | **质量自动评分** 🆕 | P1 | 7维评分/红黄绿/趋势检测 |
| **十五** | **A/B分歧模式** 🆕 | P1 | 关键节点双版本+融合 |
| **十六** | **预测-颠覆引擎** 🆕 | P1 | 60/30/10规则 |
| **十七** | **角色深度引擎** 🆕 | P0 | 信念/声纹/关系/弧线/过往/细节/成长/配角全方位塑造(四大功能+两大约束+三问检验+生命周期) |
| **十八** | **情感波动曲线** 🆕 | P1 | 全书情感编排/波形规则 |
| **十九** | **对话潜台词** 🆕 | P1 | 三层信息不对称 |
| **二十** | **章节变速引擎** 🆕 | P0 | 5种节奏类型/比例控制 |
| **二十一** | **预加载+缓存** 🆕 | P1 | 双轨缓存/60% token节省 |
| **二十二** | **批量发散模式** 🆕 | P1 | 3-5章批量/过渡阶段专用 |
| **二十三** | **Delta更新** 🆕 | P2 | 增量状态/防全量重写 |
### 模块一：角色状态库 (characters.yaml)
追踪角色当前状态，每次写到时自动校验。字段：`id, name, aliases, status(alive|dead|missing|sealed), death_chapter, death_cause, location, abilities, relationships, inventory, notes, last_appeared`。校验：死者无行动、物品唯一持有、能力增减有事件支撑、位置移动合理。

### 模块二：世界设定库 (world.yaml)
追踪地理、势力、能力体系。字段：`geography(id,name,type,location,status,destroyed_chapter), factions, power_system(name,ranks,rules)`。校验：已毁地点不再引用、势力变动有事件、能力使用不违反规则。

### 模块三：剧情线索库 (threads.yaml)
管理伏笔(planted)和收束(resolved)，防烂尾。字段：`id, description, planted_chapter/context, resolved_chapter/how, importance(major|minor|easter_egg), related_characters`。校验：major线索必须有推进、已resolved不重用、长期停滞触发遗忘警告。

### 模块四：时间线 (timeline.yaml)
按章节记录事件，防时间矛盾。字段：`chapter, events(timestamp, description, characters_involved, note)`。校验：角色不瞬移、时间流逝合理、回忆须标注。

### 模块五：世界规则 (rules.yaml)
**绝对不可违反的设定**。字段：`id, description, severity(hard|soft)`。hard 规则不可违反，soft 违反需合理理由。

### 模块六：剧情发散引擎
基于 15 种发散模式 + 记忆锚点污染生成多个剧情方向。**核心原则：文似看山不喜平。**
- **15 种模式**：D1线索咬合 | D2关系裂变 | D3禁区探索 | D4反转颠覆 | D5规则突变 | D6双线映照 | 🔑D7认知差悬念 | D8伪胜/败 | D9两难抉择 | 🔑D10倒计时压迫 | D11逆风孤战 | D12暗流涌动 | 🔑D13情感债回收 | D14渔线沉底 | 🔑**D15反常配角驱动（配角独立事件）**
- **记忆锚点污染**：发散前从 ch001 ~ ch(N-2) 扫描被遗忘的非线索细节（配角闲话、无动机行为、场景陌生物品），随机抽取 2-4 个注入种子池
- 节奏模型：四层波浪+钩子链
- 详见 `references/divergence-patterns.md`、`references/memory-anchor-usage-guide.md`（记忆锚点扫描手册）、`references/d15-example-cases.md`（D15实战案例）、`references/auto-write-workflow.md`

#### 6.1 发散去重（防雷同）

> **问题**：AI 反复用同 2-3 种模式 → 章节雷同 → 读者疲劳。

**规则**：
1. **窗口检查**：生成方向前扫描最近 5 章的 `divergence_pattern`，最近 3 章用过的模式**禁止**作为主模式
2. **多样性下限**：10 章窗口内须用 ≥6 种模式，低于 = 强制注入未使用模式
3. **组合新鲜度**：相同模式组合 8 章内不可重复
4. **评分惩罚**：模式在最近 5 章每出现一次，新奇度 -1
5. **Stale 检测**：连续 3 章共享主模式 → 下一章必须用未使用池

**未使用池注入**：10 章内 >5 种模式未使用 → 下次必须包含 ≥1 个未使用方向（优先选 D15 或记忆锚点驱动的方向）

#### 6.2 世界观格局升级引擎

> **核心问题**：主角变强了，但冲突的质地没变——数字在涨，读者已经不在乎了。

**格局的本质不是「地图有多大」，而是「主角在乎的事有多深」。**

### 6.2.1 格局升级的四个维度

格局可以在以下任一维度上扩展，不必四个维度同时升级：

| 维度 | 低格局 | → 高格局 | 升级信号 |
|------|--------|----------|---------|
| **权力** | 个人武力/技能 | → 组织/制度/规则层面的力量 | 主角不再靠拳头，而是靠「规则本身」取胜 |
| **认知** | 眼前的真相 | → 世界运作的底层逻辑 | 主角发现「我一直以为的规则，只是别人想让我看到的」 |
| **关系** | 一对一的情感纽带 | → 群体/代际/命运层面的羁绊 | 主角的选择不再只影响自己，而是影响一群人、一个时代 |
| **代价** | 个人得失 | → 文明存亡/意义本身 | 输赢不再是「我能不能活」，而是「什么值得活」 |

**四个维度的关系**：不是阶梯，是光谱。一部小说可以主要在权力维度升级（如纯战斗流），也可以在关系和代价维度升级（如情感流）。好小说通常在 2-3 个维度上交替推进。

### 6.2.2 不同流派的格局路径

| 流派 | 典型升级轴线 | 示例 |
|------|-------------|------|
| 修仙/玄幻 | 权力：外门→内门→掌门→飞升 / 认知：功法→天道→宇宙真相 | 《凡人修仙传》从七玄门到灵界 |
| 都市/商战 | 权力：个人能力→团队→公司→行业规则 / 关系：对手→盟友→利益共同体 | 从摆摊到改写行业标准 |
| 悬疑/推理 | 认知：个案真相→连环模式→体制黑幕→人性本质 | 从破一个案子到发现「所有案子都是同一个源头」 |
| 言情/情感 | 关系：好感→羁绊→命运共同体 / 代价：个人幸福→对方的人生→两难抉择 | 从「我喜欢他」到「我们的选择决定了很多人的命运」 |
| 科幻 | 认知：技术原理→文明级应用→人类定义 | 从造一个AI到「什么是人」 |
| 末世/生存 | 权力：活下去→建立据点→重建文明 / 代价：个人→团队→人类火种 | 从活着到「为什么值得活」 |

### 6.2.3 格局升级铁律

1. **不可原地踏步**：同一维度的冲突质地不可停留超过 15 章。如果主角连续 15 章在「用同样方式解决同样级别的问题」→ 格局停滞。
2. **红色药丸时刻**：每次格局跨越必须有一个信息发现——主角（和读者）同时意识到「旧世界只是更大世界的子集」。不是旁白告知，是主角亲眼看到。
3. **能力不带格局 = 数字游戏**：新能力如果不解锁新的冲突类型，只是在旧战场上赢得更快——这是最隐蔽的注水。
4. **旧冲突不可回退**：格局升级后，旧层级的敌人不再是威胁。T1 的反派到 T3 必须变盟友/路人/笑料——反复纠缠说明故事在倒退。
5. **跨维度震荡**：在主要升级维度之外，偶尔在其他维度上打开一扇窗——给读者「还有更大的东西在后面」的预感。

### 6.2.4 升级后自检

```
□ 这次升级后，主角最在乎的东西变了吗？（不仅变强了，是变了）
□ 有没有一个场景，让旧版本的主角根本无法理解现在的冲突？
□ 是否存在新的冲突类型，在此之前完全不可能出现？
□ 之前的敌人还构成威胁吗？（构成 = 格局没升级）
```

## 模块七：自动写作引擎

> 执行协议：`references/auto-writing-execution-protocol.md` | 会话恢复：`references/session-resumption-playbook.md` | 有机冲刺：§7.10 + `references/volume-outline-guide.md`

核心循环：
```
开头/种子 → 初始化项目
LOOP: 读取状态 → 发散3-5方向 → 评分选择最优 → 写章节正文 → 更新状态 → 一致性门控 → Git同步 → 终止检查(否→继续)
```

- **7.1 方向评分**：8维加权评分(节奏适配20%/翻页驱动15%/线索推进15%/情感利用10%/一致性5%/新颖度20%/钩子5%/读者惊讶度10%) + 新颖度细分评分规则 + 一致性降权补偿逻辑 + 排雷规则 + 选择策略 → `references/scoring-and-selection.md`

- **7.1.1 发散种子增强**：每次发散前额外注入两类非常规种子：
  - **#5 记忆锚点污染**：从 ch001 ~ ch(N-2) 扫描未被 threads.yaml 追踪的「被遗忘细节」——配角随口说的话、场景中看似无意义的物品、没有后续的行为动机——随机抽取 2-4 个作为发散种子
  - **#6 反常配角驱动 (D15)**：对活跃配角执行独立事件推演——配角在主角视野外做了什么，这如何间接影响主角的世界（参见发散模式库 D15）
- **7.2 一致性门控**：周期轻检/深审 + 重大事件/红灯/弧线切换触发 + 自动修复 → `references/scoring-and-selection.md`
- **7.3 循环详解**：读取→发散→评分→写作→更新→门控→Git同步→终止，8步完整流程 → `references/scoring-and-selection.md`
- **7.4 节奏自调节**：张力检测(连续同钩/高张疲劳/低张拖沓) + 弧线自动推进 → `references/scoring-and-selection.md`
- **7.5 长线管理**：跨弧线衔接 + 卷间过渡 + 终局管理 → `references/scoring-and-selection.md`
- **7.x 设定体系**：体系化设定速查 → `references/setting-system-tracking.md`
- **7.7 实操陷阱**：24条验证陷阱（含v2.1 commit_chapter工作流、字数统计、YAML引号嵌套等）→ `references/auto-write-practical-tips.md`
- **7.8 状态同步**：会话恢复必做 + 过期检测信号 + 高频/批量更新策略 → `references/auto-write-practical-tips.md`
- **7.9 方向融合**：TOP1+TOP2融合策略 + 实战案例 + 节奏参数 → `references/scoring-and-selection.md`
- **7.x 规则一致性审计**：修改规则后的跨文件一致性检查清单 → `references/rule-consistency-audit-checklist.md`

**实操 Top 7+陷阱速记**：
0. **章节正文写入策略（v4.3更新·本会话ch055-ch069全程验证）**：
   - **首选**：直接 `write_file` 调用，内容控制在 ~3000 bytes（约1000-1200中文字）以内。本会话15章全部单次 write_file 成功，零超时。
   - **备选**：若需要更长章节，两段式：write_file 前半(~600字) → patch 追加后半
   - **放弃**：terminal heredoc（中文引号与bash单引号转义频繁冲突）和 execute_code（间歇性 blocked "timed out without user response"）
   - 状态文件(YAML)可用 execute_code + write_file，不触发 stream timeout
1. **arc-tracker 每章必更新**，否则 rhythm 退化为空
2. **状态文件不会自动更新**，每5章强制回写 characters/threads/debts
3. **consistency_checker 报全绿可能是空文件**——先确认数据有意义
4. **阶段名必须与 automation-config 匹配**，否则 rhythm 返回空
5. **跨会话必须先同步再写作**——详见 `references/session-resumption-playbook.md`
6. **commit_chapter 不自动切换弧线阶段**——跨越阶段时必须手动 patch arc-tracker.yaml 的 current_phase/phase_start_chapter/phase_chapters_elapsed
7. **consistency_checker 无 --deep 参数**——`check` 自动按章节号决定深浅，`audit` 做全局审查
8. **consistency_checker 兼容 dict/list 两种 YAML 格式**(v2.1.2+)——characters.yaml 和 relationships 字段无需特定格式
9. **章节文件名必须零填充**——`ch080.md` 不是 `ch80.md`，`write_file` 路径务必用 `ch{NNN}.md` 三位格式
10. **commit_chapter.py 自动发现章节文件**(v2.2+)--省略`--count-words`时自动按`--chapter`拼接`chapters/chNNN.md`路径
11. **consistency_checker 同规则折叠**(v2.2+)--同类🟢建议>5条自动折叠为一行汇总（`--verbose`查看全部）
12. **auto_sync.py 同步检测**(v2.2+)--`auto_sync.py --dir <项目>`一键检测characters/threads/arc-tracker/timeline/debts落后情况
13. **每章写完必须 git commit & push**(v2.4+)——commit 消息格式 `ch{NNN}: 标题`，卷完结加 `[卷完结]`，全书完结加 `[全书完结]`
14. **跨会话恢复先 git pull**(v2.4+)——其他会话可能已推送新章节，不 pull 就写会产生冲突
15. **每章前300字必须有冲突/行动**(v2.5+)——参见 §9.1 叙事克制，环境描写、背景回顾开篇都是违规
16. **大高潮超过5000字必须分拆**(v2.5+)——参见 §9.2，一口气写完高潮是叙事资产浪费
17. **前10章主角被动反应不超2章连续**(v2.5+)——参见 §9.3 能动性原则，2章连续被动必须下章补主动，3章连续一票否决
18. **commit_chapter.py --thread-advance 对不存在的线索ID静默跳过**——显示"⚠️ 线索 XXX 未找到，跳过"但不报错。提交前确认线索ID与threads.yaml一致；含"+"号的复合线索名会被跳过（parser局限）
19. **volume_transition.py 只做机械重置，不创建大纲/角色/线索**——后续8步手动清单见§7.5.1，遗漏任何一步都会导致新卷写作卡壳
20. **章节写入方法选择**——直接 `write_file` 是首选（~3000 bytes内稳定），不要先跳到 execute_code 或 heredoc。仅当内容超过此上限才用两段式（write_file前半+patch后半）。terminal heredoc 中文引号与bash `<< 'EOF'` 的单引号转义冲突频繁，不推荐。
21. **read_file 行号前缀污染**——`read_file()` 返回 `"N|内容"` 格式（带行号前缀），如果将此输出拼接到新文件内容中再 write_file，YAML/代码语法必毁。**绝对禁止 read→拼接→write 回环**。全量重写用 `terminal heredoc`（`cat > file.yaml`）或 `execute_code` 内直接 `write_file` 全量覆盖，不要读取旧内容拼接。
22. **YAML 状态文件用 terminal heredoc 写入**——characters.yaml/threads.yaml 等状态文件的全量重写，用 `terminal` 命令 `cat > path.yaml` 的 heredoc 方式最可靠（避免 Python 引号嵌套和 read_file 污染）。多角色时分2-3次 `cat >>` 追加。每次写完后 `python3 -c "import yaml; yaml.safe_load(open('file'))"` 验证。
23. **单次 write_file 是章节正文写入的首选方法**（v4.3更新·15章实战验证）——直接调用 `write_file(path, content)` 是最高效可靠的方式，本会话 ch055-ch069 全部成功。内容控制在 ~3000 bytes以内（约1000-1200中文汉字），直接一次写入。如需更长的章节，用两段式：`write_file` 写前半（~600字）→ `patch(mode='replace')` 找到前半末尾唯一锚点字符串，用 `new_string` 替换为「原锚点 + 后半全部内容」。锚点通常取章节最后一句对白或动作描写。patch 的 content-length 容忍度远高于 execute_code。⚠️ 不要在 Python 三引号字符串内嵌入 `\f`（form feed）——会导致 SyntaxError。Fallback：execute_code 连续失败时再考虑 terminal heredoc，但 heredoc 的中文引号与 bash 转义冲突频繁（`<< 'EOF'` 内不能嵌入 `'`）。
24. **卷间 characters.yaml 全量重写必须用 heredoc**——不要在 execute_code 内用 read_file 读取再拼接——行号前缀会永久污染文件，正则清理也不可靠（多行压缩/缩进丢失）。正确做法：`terminal` heredoc `cat > characters.yaml` 分批写入每个角色。
25. **章节正文必须通俗易懂，严禁大段专业术语**（网文读者不吃这套）。
    - **技术向禁令**：禁止在叙事中直接使用"源代码/debugger/打补丁/BUG/编译/代码/分号"等程序员术语作为核心隐喻。主角可以是程序员背景，但叙事语言必须翻译成娱乐向——"源代码"→"规则原本"、"debugger"→"破局者/下棋的人"、"打补丁"→"修正/改写"、结尾"分号"→"问号"。商业/博弈类隐喻（拼刀刀/杀熟/风控模型/复利飞轮）仍可用——这些是网文读者能get到的概念。纯粹编程术语只在主角内心独白中偶尔出现，不出现在叙述者视角。
    - **设定术语约束**（§9.4）：单段首次新术语≤3个；新术语出现后300字内必须有行动证实；禁止超过150字纯设定说明段落
    - **三不写检查**：不写纯设定段落 / 不写名词轰炸 / 不写体系说明文
    - **优先级**：当设定完整性和叙事流畅性冲突时，**叙事流畅性永远优先**。少解释一个功法原理不会让故事崩，但停下来解释500字会让读者跳走。
26. **terminal heredoc 只在 execute_code 连续失败时作为最后手段**——`cat > chapters/chNNN.md << 'EOF'` 的致命缺陷：heredoc 内不能嵌入单引号 `'`（中文角色名、对话中的引号大量使用），bash 转义频繁冲突。本会话15章全部用直接 `write_file` 成功，未使用 heredoc。仅在 write_file 和 execute_code 都连续超时时才考虑 heredoc，且必须检查内容中无单引号或手动转义。——本会话未触发
27. **卷终反转严禁标准套路**——网文读者对以下套路已审美疲劳，必须避开：①"地底还有更古老的怪物/更大的威胁" ②"幕后还有更大的黑手" ③系统本身是活的/有意识。这些不是反转——是读者在第二章就能猜到的标准答案。替代方向须从**世界观底层认知**做文章：重新定义核心资源的本质（灵脉不是矿→是执念）、重新解释反派的动机（杀熟不是压迫→是保护）、让读者发现"整个世界运作的逻辑从一开始就被误解了"。详见 `references/reversal-patterns.md`。
28. **git commit 消息含中文必须用单引号**——`git commit -m "中文标题"` 双引号会导致中文在 bash 中产生不可见字符，git push 时标题显示为乱码。正确做法：`git commit -m 'ch069: 写入'` 单引号包裹中文消息。
29. **`auto_write.py wordcount` 有浮点进度条 bug**（v4.2.1已知）——`bar = "█" * bar_len` 在 `bar_len` 为 float 时抛出 `TypeError`，导致字数明细无法打印。顶部的汇总数据（章节数/总字数/篇均/距离目标）仍然正确输出，不受影响。workaround：跳过 `--update`，用 `wc -m chapters/ch*.md | tail -1` 手工统计字符数。
30. **`volume_transition.py` 的 `last_volume_end_chapter` 可能落后实际**——脚本从 `novel-project.yaml` 读取 `current_chapter` 作为卷末章节号，但如果该字段在上卷冲刺中未及时更新（常见于批量push滞后），过渡章节号会偏小。查实际章节：`ls chapters/ | tail -1`，以文件系统为准，手动 patch `arc-tracker.yaml` 修正。

### CLI 工具速查（v2.1 新增）

| 命令 | 用途 | 示例 |
|------|------|------|
| `commit_chapter.py` | 一键章后状态更新（替代4-6次patch） | `--dir . --chapter 42 --words 3500 --pattern "D5+D9" --hook 焦虑型 --tension 8` |
| `volume_transition.py` | 卷间机械状态重置+AI待办清单 | `--dir . --volume 5 --title "根" --first-chapter 71` |
| `auto_write.py wordcount` | 扫描全部章节统计中文字数 | `--dir . [--update]`（无--update仅打印） |
| `auto_write.py score --mode 3dim` | 3维简化评分(节奏40%/线索35%/安全25%) | `--d1 "D7,4,5,4"` |
| `consistency_checker.py --verbose` | 显示全部建议含已抑制的 | 默认抑制连续3次🟢建议+同规则>5条折叠 |
| `auto_sync.py --dir .` | 检测状态文件同步差距 | 输出characters/threads/arc-tracker/timeline落后章数 |

`commit_chapter.py` 高级用法：
- `--count-words chapters/ch042.md` 自动统计中文字数，覆盖 `--words`（v2.2+：省略此参数时自动按`--chapter`拼接路径）
- `--thread-advance "T015:developed,T018:resolved:42"` 批量推进/收束线索
- `--dry-run` 预览变更不写入

### 7.10 有机冲刺模式（大纲驱动）

当卷大纲足够详细时，跳过 CLI 直接有机选择方向，速度是脚本模式的2-3倍。

**前提**：✅6大种子(主线×1+谜题×2+抉择×2+情感×1) ✅弧线阶段表 ✅设定速查表 ✅前卷状态已完成过渡

**零CLI循环（v2.2 commit_chapter 驱动）**：

> ⚠️ **核心原则：用户说"继续"=不间断冲刺。不要暂停问问题、不要输出统计表、不要确认。写就完了。**

```
1. 心算确认阶段 → 选择发散模式
2. write_file: 写章节正文 ch{NNN}.md（路径必须三位零填充！ch080.md ✅ ch80.md ❌）
3. commit_chapter.py --dir DIR --chapter N --title "标题" \
     --pattern "D5+D9" --hook 焦虑型 --tension 8 \
     [--thread-advance "T015:developed"]
   ↑ --count-words 已省略，v2.2+自动拼接 chapters/chNNN.md
4. 每3-5章: consistency_checker.py check DIR
5. 每5章: patch characters.yaml + emotional-debts.yaml
6. 每3-5章批量 git push: git add -A && git commit -m "ch{NNN}-{MMM}: 标题批" && git push origin main
   ↑ 批量提交比逐章push更高效。commit消息可用范围或最终章号标注
   ↑ 单章commit格式：ch{NNN}: 标题；批量：ch{NNN}-ch{MMM}: 批次提交
7. 继续下一章（不停顿、不问用户）
```
实测：90章连续写作，零手动patch，零一致性冲突

**冲刺模式字数管理**：连续冲刺10+章时字数会在1500-4500之间波动。稳定方法：
- 大纲每章标注预估字数（如"本章3000字+关键场景"）
- 每5章用 `auto_write.py wordcount --dir <目录>` 校准
- 字数缩水严重时在下一章方向中加入细节类模式（D6双线映照/D13情感回收）

**批量写作优化**（实测效率提升2-3倍）：连续冲刺时，可一次 invoke 2-3个 `write_file`（ch080/ch081/ch082），然后用一次 `commit_chapter.py` 逐章提交，最后批量 `git push`。减少上下文切换开销，但注意：
- 批量写作会降低每章的断章自检质量，高风险章节（高潮/反转/角色死亡）仍应单章写
- 批量 git push 的 commit 消息用范围格式：`ch080-ch082: 批次提交`

**与脚本模式切换**：卡住→回退CLI | 跨卷→必须CLI做过渡

**卷完结流程**：
1. `consistency_checker.py audit <目录>` 全局审查
2. `auto_write.py wordcount --dir <目录> --update` 精确字数
3. 检查 threads.yaml 全部 major 线索已 resolved
4. patch arc-tracker.yaml current_phase 为最终阶段
5. 输出完本统计表（章节/字数/线索收束/冲突数）

详细指南：`references/volume-outline-guide.md`

冲刺模式可通过 automation-config.yaml 的 sprint 部分启用。设置 sprint.enabled: true 跳过 rhythm/score，consistency 间隔自动放宽。

### 7.5.1 卷间过渡完整清单

> `volume_transition.py` 只做机械重置（arc-tracker/consistency-log/novel-project），以下手动步骤 **必须全部完成** 才能开始新卷写作：

```
1. ☐ 创建卷大纲 outlines/volumeN-outline.md
   - 六大种子（主线×1 + 谜题×2 + 抉择×2 + 情感×1）
   - 弧线阶段规划表（阶段|章节范围|核心事件|张力|发散模式）
   - 前5-10章逐章规划（模式/核心/钩子/张力）

2. ☐ 全量重写 characters.yaml（用 terminal heredoc `cat >`，不要 execute_code+read_file 拼接——行号前缀会污染文件）
   - 保留存活角色（更新 last_appeared/condition/abilities/relationships）
   - 新增本卷新角色（每个至少: id/name/status/abilities/power_level/relationships/key_traits）
   - 移除不再出场的角色或标记 status=absent
   - 每个 relationship 标注 since_chapter
   - 多角色时分2-3次 `cat >>` 追加，每次写完 `python3 -c "import yaml; yaml.safe_load(open('file'))"` 验证

3. ☐ 全量重写 threads.yaml
   - 封卷旧线索：status=resolved, payoff_chapter=实际收束章节
   - 播种新线索：至少3条 active（1 main + 1 puzzle + 1 character_arc）
   - 每条新线索必须有 planted_chapter/last_updated_chapter/key_points
   - 保留旧线索（status=resolved）供一致性校验回溯

4. ☐ 全量重写 emotional-debts.yaml
   - 结转未偿还的旧债（status=outstanding/partially_paid）
   - 播种新情感债（每卷至少2-3条）

5. ☐ 更新 world.yaml
   - 新增本卷场景（每个至少: id/name/type/location_type/description/atmosphere/danger_level）
   - 新增世界规则（如有新设定的硬规则）
   - 保留旧场景（可能跨卷复用）

6. ☐ 更新 timeline.yaml
   - 添加卷分界标记（chapter=上卷末/下卷首, event="卷N→卷N+1过渡"）
   - 清空本卷事件区（写作中更新）

7. ☐ 一致性确认
   - 运行 consistency_checker.py check 确认无硬冲突
   - 确认 arc-tracker.current_phase 与 automation-config 匹配

8. ☐ Git 提交
   - git add -A && git commit -m "state: 卷N→卷N+1过渡+状态文件全量更新 [卷N+1准备]"
   - git push origin main
```

**常见遗漏**：忘记更新 `since_chapter` 字段→关系时间线错乱；新线索缺 `key_points`→后续章节推进无锚点；world.yaml 缺 `danger_level`→禁区探索模式无参考基线。

### 7.6 与人工模式的切换

| 场景 | 操作 |
|------|------|
| 自动→人工 | 设 `mode: manual` 或说"暂停自动写作" |
| 人工→自动 | 设 `mode: auto` 或说"继续自动写作" |
| 单章接管 | 生成方向后说"这章我来选" |
| 一键全自动 | 初始化后说"全自动写完" |

---

## 使用流程

### 模式A：全自动写作
1. **会话恢复**：确认章节数与 novel-project.yaml 一致，不同步则先批量更新 → `references/session-resumption-playbook.md`
2. **初始化**：`auto_write.py init --name "书名" --dir ~/novels/my-novel --mode auto`
3. **提供开头**：AI 自动提取设定→填充状态→进入循环
4. **自动循环**：每章输出 📝完成/🎭模式/🎣钩子/📊进度/✅一致性
5. **章后提交**：`commit_chapter.py` 一键更新 novel-project/arc-tracker/threads/consistency-log（替代手动patch）
6. **中途干预**：随时"暂停"/"用D9"/"角色不该死"/"节奏太慢"/"跳到高潮"
7. **卷间过渡**：`volume_transition.py` 重置状态 + AI 手动完成大纲/角色/线索重写
8. **完成输出**：全书摘要 + 线索收束 + 一致性审计 + `wordcount --update` 精确字数

### 模式B：人机协作
1. **初始化**：`novel_state.py init --name "书名" --dir ~/novels/my-novel`
2. **写前校验**：`consistency_checker.py check ~/novels/my-novel`
3. **发散+评分**：AI 生成3-5方向含评分，你选方向，AI 写章节
4. **写后更新**：AI 自动提取事件→更新yaml→校验
5. **定期审查**：`consistency_checker.py audit ~/novels/my-novel`

---

## 模块八：开篇冲击力引擎（第1-2章专用）

> **铁律：读者给一本新小说的耐心只有两章。**

第一章必须同时完成三件事：**即刻冲突**（开篇就有危机/异常）、**角色锚定**（5分钟知主角是谁）、**悬念炸弹**（章末读者无法不追问）。

第二章任务：**冲突升级 + 情感投资 + 认知翻转 + 次级悬念**。

第1-2章评分权重调整：翻页驱动35%/钩子强度25%。一票否决：翻页驱动≤2、钩子强度≤2、前500字无冲突、零爽点。

> 完整规则：死亡模式/评分公式/写后审查清单/5种钩子模板 → `references/opening-impact-engine.md`

---

## 模块九：叙事纪律与读者情绪管理

### 9.1 叙事克制（全章通用）

- **300字入局**：每章前300字必须进入冲突/行动，禁止环境描写/背景回顾开篇。
- **背景融入**：禁止超过200字的纯设定段落。用"主角需要这个设定"的场景引入，而非"作者想告诉读者"。
- **目标驱动**：主角从第一章起必须有近期目标。3章内无主动行动 → 补上主动目标。

### 9.2 断章与高潮控制

- **在该断的地方断**：好章结尾是"事情正到最关键处"，不是"事情解决了"。断章黄金位置：决策前/真相前/冲突前/不安信息刚出现/角色踏入危险不知情。
- **大高潮分拆**：超过5000字→分拆为2-3章（高潮前夜→高潮爆发→高潮余波）。
- **断章自检**：读者感觉"好吧"非"然后呢？"→失败。好的断章让读者带着1-2个未解问题离开。

### 9.3 早期情绪保护（前10章）

**前10章不虐主**。红线：彻底击败无反击/剥夺核心能力/严重羞辱无反击/重要盟友死亡。连续2章被动→下章必须主动（第3章被动=一票否决）。

前10章主动行动占比≥60%。爽度密度加倍（每2章≥1中爽点）。第10章必做情绪审查。

### 9.4 通俗易懂原则（全书通用）

> **核心问题**：AI 写作容易陷入"解释癖"——为了让设定看起来完整，堆砌大段术语、功法原理、势力层级介绍。网文读者不需要这些，需要的是**在行动中感知设定**。

**三不写**（否决级）：
- 🚫 **不写纯设定段落**：连续超过150字纯阐述世界观/功法/势力/历史，无角色行动穿插 → 违规
- 🚫 **不写名词轰炸**：单段内首次出现≥3个生造术语（功法名/地名/势力名/境界名）且无上下文消化 → 违规
- 🚫 **不写体系说明文**：用旁白把一套完整体系（修行体系/规则体系/力量等级表）一次性讲完 → 违规

**三要写**（执行级）：
- ✅ **设定融入行动**：设定信息在场景中"露出来"——角色做出选择时顺便带出规则，敌人展示能力时带出体系，环境描写带出地理，**而不是让叙事停下来讲解**
- ✅ **名词即生即用**：新术语出现后，300字内必须看到它在行动中发生作用（打斗、对话、推理、抉择），否则读者记不住
- ✅ **优先级穿透**：当【设定完整展示】与【叙事流畅性】冲突时，**叙事流畅性永远优先**。如果为了保持节奏而需要牺牲某个设定细节，写章后在 threads.yaml 记一笔即可

**自检清单**（每章写作时自查）：
```yaml
□ 本章是否有超过150字的纯说明段落？（有→重写，拆散融入行动）
□ 本章首次出场的新术语是否≤3个？（>3→削减或推迟介绍）
□ 每一个新术语是否在300字内有行动证实？（否→补一个使用场景）
□ 删掉某个设定说明，故事节奏是否更流畅？（流畅=说明多余）
□ 本章可以被「对设定一无所知」的读者完全看懂吗？
```

> 完整规则：300字入局/背景融入判定标准/高潮分拆节奏表/10章情绪审查清单 → `references/narrative-discipline.md`

---

## 模块十：爽度引擎 + 画面感引擎

### 10.1 快节奏四原则

| 原则 | 规则 | 违规检测 |
|------|------|---------|
| **零废段** | 每段至少完成：推进剧情/制造冲突/揭示信息/创造爽点 | 删掉不影响主线=废段 |
| **场景快切** | 场景切换不超过3句过渡 | 超过3句=压缩 |
| **对话即角力** | 每句对话推进信息/施压/反击/暴露弱点。纯寒暄≤2句 | 超过3句纯寒暄=砍 |
| **行动代心理** | 用行动和对话展现内心。心理独白>150字且无冲突穿插=违规 | 拆散分布到行动中 |

### 10.2 爽度引擎

**六大爽型**（每5章≥3种）：装逼打脸/逆风翻盘/智商碾压/实力碾压/夺宝抢机缘/人心归附

**密度规则**：每3章≥1中爽点 | 每5章≥1大爽点 | 弧线高潮章≥1极爽点 | 连续2章零爽→违规告警（3章零爽=一票否决）

**爽点三步**：压制/困境（憋气）→ 转折/蓄力（合理翻盘，有伏笔支撑）→ 释放/碾压（快准狠，2段内完成）

**旁观者三层**（大爽点≥2层）：①直接对手崩溃/恐惧 ②同级旁观者震惊/重新评估 ③高手/前辈动容/忌惮

### 10.3 画面感引擎

**六大技法**：

| 技法 | 核心 |
|------|------|
| 镜头语言 | 远景/中景/特写/推/拉/跟拍，重要场景「远→中→特」切换 |
| 感官五重 | 重要场景≥3种感官，高潮5种全满。禁用「他很害怕」→ 用身体/感官说情绪 |
| 动作微分解 | 关键动作拆成1-3秒慢动作，适用一招制敌/关键反转/死亡瞬间 |
| 环境投射 | 用天气/光线/空间说情绪。压迫→低矮天花板，觉醒→光线涌入 |
| 物件象征 | 关键物件走完三阶段：首次出现（功能引入）→再次出现（叠加情感）→最终出现（封印使命） |
| 声画对位 | 声音和画面矛盾时张力最大。身后人声鼎沸，他只听见心跳 |

### 10.4 爽度+画面感联合规则

- **铁律一**：爽点必须有画面（≥2种技法呈现）。「他一拳打倒对手」=新闻，不是小说。
- **铁律二**：画面必须服务叙事。删掉后爽感不减的描写=废画面。
- **冲突优先级**：爽度 > 画面感 > 文笔修饰

### 10.5 每章自检

```
□ 【爽度密度】3章内≥1中爽？5章内≥1大爽？
□ 【三步构建】压制→转折→释放完整？
□ 【旁观者】大爽点≥2层旁观者反应？
□ 【类型轮换】近5章类型有变化？（不可连3次同类型）
□ 【镜头】「远→中→特」或「特→中→远」镜头流程？
□ 【感官】重要场景≥3种感官？高潮5种？
□ 【环境】环境描写服务情绪？删掉后受损？
□ 【物件】关键物件重复出现并叠加含义？
```

> 完整技法：六大爽型详细写法要点/反模式、六种镜头类型含示例、感官五重对比表、动作微分解示例、环境情绪投射表、物件象征三阶段写法、声画对位示例 → `references/satisfaction-cinema-details.md`
---

### 7.11 跨媒介叙事适配（v4.0新增）

写的时候考虑「这章能被改成漫画/动画/剧集吗」。每章至少1个**名场面**——可被截图传播的瞬间；每3章至少1个**金句**——读者会划线分享的台词。

---

### §7.12 大纲漂移检测（P2·每10章触发）

> 大纲是罗盘，不是牢笼。漂移是正常的，关键是你知道在漂。

**每10章检测**：
1. 对比 automation-config.yaml 预期 vs 实际章节内容
2. 如果故事更好 → 更新大纲，记录漂移方向
3. 如果主要节拍丢失 AND 故事变弱 → 重新对齐
4. 有价值但时机不对的节拍 → 作为 `orphaned_beat` 加入 threads.yaml

**卷完结时**：所有 orphaned_beat 必须决定——融入/搁置/明确放弃。

---

## 模块十一：散文质量层（P1·中文网文版）

> **核心前提**：这是网文，不是纯文学。散文质量服务节奏和爽度，而非文学野心。

### 11.1 五感平衡

| 感官 | 目标占比 | 说明 |
|------|---------|------|
| 视觉 | ~40% | 仍占主导，但不再垄断 |
| 听觉 | ~25% | 声音、寂静、对话节奏 |
| 触觉 | ~20% | 质感、温度、身体感受 |
| 嗅觉 | ~10% | 最强情绪触发感官——用来强化情感记忆 |
| 味觉 | ~5% | 在高潮/亲密场景尤其有效 |

**规则**：每章≥3种感官，新场景入场头500字5种全触发，打斗场景≥4种（视觉+听觉+触觉+嗅觉必含）。

### 11.2 句式变化

- **长度变化**：3句+长句（>15字）后插1短句（<8字）。短句制造冲击。
- **开头变化**：禁止3句+连续以相同结构开头（主-谓、人称代词、「XX是XX」）。
- **段落节奏**：禁止4段+连续纯叙述无对话/动作。
- **战斗句式**：打斗场景句子≤15字——短句=速度=冲击。

### 11.3 Show vs Tell

**铁律**：POV角色主要情绪≥60%用身体/动作/环境展示，不直接命名。

| ❌ 讲述 | ✅ 展示 |
|--------|--------|
| 她很愤怒。 | 她的指节在茶杯上攥得发白。瓷器发出细碎的悲鸣。 |
| 他感到恐惧。 | 他吐出的那口气变成了嘶哑的口哨。背已经贴到了墙上。 |

**红旗**：「感到XX」「很XX」「觉得XX」连续出现3次 → 强制修订。

### 11.4 对话工艺

- **交锋式**：每句对话推信息/施压/反击/暴露弱点。纯寒暄≤2句。
- **动作节奏**：≥50%的对话标签用动作（而非「XX说」）。
- **人物声纹**：每个角色有辨识度——词汇量/句长/口头禅/回避话题。
- **禁止解说式对话**：「如你所知……」型。

### 11.5 修辞新鲜度

- **每2000字≥1个新鲜比喻**，每章最多3个。
- **禁止陈词**：禁用「快如闪电」「冷若冰霜」「心如刀割」等所有成语级比喻。
- **比喻域统一**：同一段内不混用多域比喻。

> 完整规则含示例/反模式/自检清单 → `references/prose-quality-guide.md`

---

## 模块十二：章节解剖引擎（P0·每章必检）

> **核心问题**：AI能把情节节点总结成500字，但不知道怎么扩展成完整章节。

### 12.1 章节八元素（每章≥5/8）

| # | 元素 | ~字数 | 作用 |
|---|------|-------|------|
| A | **入局钩子** | 100-200 | 半途入局——动作开篇/感官冲击/对话直入。无铺垫。 |
| B | **行动序列** | 400-600 | 核心事件。战斗/谈判/发现/对峙。Show，不Tell。 |
| C | **反应段落** | 200-300 | 主角消化刚发生的事。身体/情绪/策略三重反应。 |
| D | **后果涟漪** | 200-300 | 行动如何改变局面？他人如何反应？新问题从旧答案中诞生。 |
| E | **配角时刻** | 200-300 | 非POV角色有意义的行动/发言。展示世界不围着主角转。 |
| F | **世界质感** | 100-200 | 感官细节/环境描写——**融入行动，非独立段落**。 |
| G | **子线推进** | 200-300 | 至少1条副线向前移动一步。 |
| H | **断章钩子** | 100-200 | 悬念/反转/追问/情感重击。**迫使读者点开下一章**。 |

### 12.2 六大扩容技法

当章节太薄时按顺序注入：**反应场景** → **旁观者视角** → **感官铺层** → **微型闪回** → **势力涟漪** → **代价对话**

### 12.3 章节密度规则

- 2+场景 / 2+角色有意义互动 / 1+感官变化 / 1+新信息 / 1+决策做出
- 缺失3/5要素 → 章节太薄，无论字数多少。

> 完整扩容技法含示例 → `references/chapter-anatomy-engine.md`

---

## 模块十三：伏笔自动发现引擎（P0）

### 13.1 写后扫描

每章写完后AI主动扫描「可伏笔化内容」：人物语焉不详 / 物件首次出现 / 能力未充分解释 / 配角异常行为 / 环境暗示。

### 13.2 自动入库

扫描到后 → 自动向 threads.yaml 添加线索（`status: auto_planted`）。

### 13.3 回旋镖追踪

每10章扫描已收束线索，看能否意外复用——「这个伏笔已经收了——但如果它其实还有另一层含义呢？」

---

## 模块十四：章节质量自动评分（P1）

### 14.1 七维评分（每章写完后）

| 维度 | 权重 | 标准 |
|------|------|------|
| 节奏 | 20% | 入局速度/场景切换/断章钩力 |
| 爽度 | 20% | 爽点密度/类型/构建完整性/旁观者层数 |
| 画面感 | 15% | 感官多样性/镜头变化/环境互动 |
| 对话质量 | 15% | 交锋性/潜台词/人物声纹区分 |
| 角色区分度 | 10% | 不同角色说话做事风格不同 |
| 情感冲击 | 10% | 3章窗口内的情绪波动幅度 |
| 结构完整度 | 10% | 8元素覆盖/密度/断章质量 |

### 14.2 标记系统

- 🟢 **≥7.5** → 合格，继续冲刺
- 🟡 **6.0-7.4** → 标记关注
- 🔴 **<6.0** → 触发修订建议

### 14.3 滑坡警告

连续3章总分下滑 → 暂停冲刺，诊断（模式枯竭/节奏疲劳/角色停滞）。

---

## 模块十五：A/B分歧模式（P1·关键节点专用）

**触发**：角色死亡/重大反转/卷终高潮/主角重大抉择。

**流程**：生成A版（主流方向）+ B版（颠覆方向）→ 融合A的优点和B的优点 = 融合版C → 记录决策原因。

---

## 模块十六：读者预测-颠覆引擎（P1）

### 16.1 三层追踪

| 层级 | 概率 | 状态 |
|------|------|------|
| L1 确定知道 | 90%+ | 前文明确展示 |
| L2 强烈怀疑 | 60-80% | 有伏笔暗示未确认 |
| L3 隐约感觉 | 30-50% | 氛围/类型惯例暗示 |

### 16.2 60/30/10 规则

每10章事件结局：**60%满足预期**（猜对但写得精彩）+ **30%颠覆预期**（猜错但逻辑自洽）+ **10%完全意外**（合理不突兀）。

反模式：100%满足=好猜；100%颠覆=为反转而反转。

### 16.3 卷终反转设计（P0·卷完结前必检）

> 完整设计指南：`references/reversal-patterns.md`

卷终反转是全书最大的悬念炸弹。禁止使用"更大的怪物/幕后黑手/系统觉醒"等标准套路。替代方向必须是**世界观认知反转**——重新定义核心资源的本质、重新解释关键角色的动机、让读者发现"整个世界的运作逻辑从一开始就被误解了"。

**自检**：卷终反转能用一句话概括吗？如果概括里有"更"字——换个方向。

---

## 模块十七：角色深度引擎（P0）

### 17.1 信念系统（characters.yaml新增字段）

```yaml
belief_system:
  core_belief: "我相信什么（可能是错的）"
  deepest_fear: "我最怕什么"
  deepest_desire: "我最渴望什么"
  moral_line: "我绝不会做的事"
  self_deception: "我对自己说的谎"
```

**每卷至少1次信念挑战**——让角色面对与核心信念相悖的局面。

### 17.2 声音指纹

```yaml
voice_fingerprint:
  vocab_level: simple|normal|literary|technical
  sentence_length: short|medium|long|mixed
  signature_phrases: ["口头禅1", "口头禅2"]
  topics_gravitate: ["喜欢聊的"]
  topics_avoid: ["回避的"]
  speech_quirk: "独特说话方式"
```

**自检**：遮住角色名看对话——能分辨是谁在说话吗？

### 17.3 关系动态图

追踪关系的**张力和变化方向**（非简单「谁和谁是朋友」）：

```yaml
relationship_dynamics:
  - pair: ["主角", "导师"]
    surface: "尊敬/服从"
    subtext: "主角怀疑导师隐瞒了什么"
    trajectory: cooling  # warming/cooling/stable/volatile
```

### 17.4 角色弧线阶段

每个角色追踪阶段：`setup → build → crisis → transformation → resolution`。

### 17.5 人物过往（"填过往"技法）

> **核心原则**：没有无缘无故的善恶选择——每个人物的言行举止，都源于他的过往经历。过往四大来源：童年印记 | 原生创伤 | 执念遗憾 | 成长转折点。

**释放策略**：**千万不要一次性全盘托出**。分3-5次逐步释放，每次释放解答一个疑点又引出新疑问——既解释人物动机，又留有悬念让读者继续探究。释放时机：

| 时机 | 示例 |
|------|------|
| 面临相似情境 | 被背叛时→触发被背叛记忆 |
| 被迫做出选择 | 选择本身暴露过去 |
| 他人提及相关话题 | 反应异常引起注意 |
| 关键时刻闪回 | 战斗/决断前0.5秒回忆 |

**避坑**：回忆必须推动剧情或塑造人物——无意义回忆一律删除。每章最多1次回忆闪回。

### 17.6 人物细节（"加细节"技法）

> **核心原则**：动作的微表情、神态的细微变化、内心的挣扎纠结，远比直白的"他很生气""她很伤心"更有力量。这是画面感在角色层面的落地。

**三层次细节**：

| 层次 | 内容 | 密度规则 |
|------|------|---------|
| 动作微表情 | 手指攥紧/松开、瞳孔变化、喉结滚动 | 关键对话≥1层 |
| 神态变化 | 眼底温度升降、嘴角弧度、呼吸节奏 | 情感转折≥2层 |
| 内心碎片 | 1-2句碎片化心理活动（非长篇独白） | 高潮场面3层全开 |

**经典示例**：❌ "他不会骗我吧？"→ ✅ "他目光掠过她袖口里因紧张而攥紧的手指，眼底温度降了几分，语气却一如寻常：'你不会骗我的，对吗？'"

**避坑**：不必每句话都加动作——对抗性场面细节加倍，信息交换场面细节减半。只加对剧情和人物有意义的细节。

### 17.7 成长逻辑（"添成长"技法）

> **核心原则**：鲜活的人物从来不是一成不变的。无论主角还是配角，都会随着剧情发展而成长。

**成长四步链**：
```
困境/冲突 → 挣扎/反思 → 改变/突破 → 沉淀/蜕变
```

**反模式**：「无理由成长」（没有触发事件就变了）、「一蹴而就成长」（一章内完成蜕变）。

**三类角色成长线**：

| 类型 | 核心 | 示例轨迹 |
|------|------|---------|
| **主角成长（核心线）** | 贯穿主线的性格蜕变 | 自私→担当 / 懦弱→勇敢 / 迷茫→坚定 / 孤傲→有情 / 摆烂→开卷 |
| **配角成长（辅助线）** | 衬托主角+丰富故事层次 | 冲动徒弟→可靠助力 / 任性妹妹→温暖家人 |
| **反派成长（反向线）** | 不是"洗白"，是让恶有逻辑有根源 | 偏执→醒悟 / 被操控→找回自我 / 复仇→为救赎牺牲 |

**成长梯度铁律**：
- 懦弱的人不会突然无所畏惧——只会慢慢变勇敢
- 不是所有人都需要"完美成长"——有遗憾有不足更贴近真实
- 每次成长必须有对应困境/冲突作为触发事件
- 成长后允许偶尔反复（主角成长后依然会偶尔迷茫，只是学会了快速调整）

### 17.8 配角全方位塑造引擎（"赋灵魂"技法 v2.0）

> **核心原则**：配角不是"工具人"。先明确功能定位，再展开人设。创作时用必要性三问检验，审查时用自检清单诊断。完整示例和反模式 → `references/supporting-character-engine.md`

#### 17.8.1 配角四大功能定位

每个配角必须在以下至少一个维度上承担不可替代的功能：

| 功能 | 核心作用 | 典型角色 | 判断标准 |
|------|---------|---------|---------|
| **① 推动剧情** | 提供关键信息、触发核心事件、制造冲突 | 引路人、任务发布者、背叛者、目击者 | 删掉他 → 关键剧情节点缺少触发者 |
| **② 镜像与衬托** | 与主角对比，凸显主角性格特质 | **正向衬托**（华生→福尔摩斯）/ **反向对照**（懦弱→勇敢） | 删掉他 → 主角某方面特质失去参照物 |
| **③ 代表世界观** | 言行举止、阶层命运展现背景设定 | 底层贫民、贵族纨绔、异族流亡者、旧时代遗民 | 删掉他 → 世界观的某一面失去具体化身 |
| **④ 情绪调节器** | 调节叙事节奏和读者情绪 | **喜剧担当** / **情感支柱** / **士气鼓舞者** | 删掉他 → 故事情绪单一、缺乏调节阀 |

**功能组合规则**：重要配角≥2种功能 | 核心配角≥3种 | 次要配角1种即可 | 同功能类型≤2个

#### 17.8.2 配角两大约束

**约束一：独特性 — 高辨识度，拒绝面目模糊**

| 技法 | 规则 | 检验 |
|------|------|------|
| **鲜明标签** | 1个最鲜明的视觉/语言/行为特征 | 3秒内能想起？ |
| **极致性格** | 只放大1-2种性格特质到极致 | 只用一个场景就能展现？ |
| **标签克制** | 好标签1个 > 模糊标签3个。出场只展示1个，后续叠加 | 读者分得清谁是谁？ |

**约束二：独立性 — 他的人生不是主角的挂件**

| 技法 | 规则 | 检验 |
|------|------|------|
| **独立动机与欲望** | 配角有自己的生活目标、利益诉求和恐惧。目标可以很小，但必须是**他自己的** | 能不提到主角就写出他的欲望？ |
| **逻辑自洽的行动** | 帮助或背叛主角，必须基于自身利益/性格/信仰，而非「作者需要」 | 「为什么是他来做？」→ 能从性格/利益/过往找到答案？ |
| **独立时间线** | 配角不在主角身边时，生活在正常运转 | 「上次见面到现在他经历了什么？」→ 能答出具体场景？ |

#### 17.8.3 配角必要性三问（每卷至少一次）

| # | 问题 | 不合格 → | 合格 → |
|---|------|---------|--------|
| **1** | **删掉他，主线剧情还能走通吗？** | 可能冗余，考虑合并或删除 | 明确他的不可替代节点 |
| **2** | **他对同一件事的看法和主角有什么不同？** | 主角的复读机，缺乏独立人格 | 深化分歧，制造戏剧张力 |
| **3** | **他不跟主角待在一起时在忙什么？** | 缺乏生命力，主角的影子 | 丰富离线时间线 |

**审查频率**：配角出场时 | 每卷结束时 | 主角团超过5人时强制审查

**冗余处理**：三问全不合格 → 删除/合并 | 两问不合格 → 深化改造 | 一问不合格 → 针对性补齐

#### 17.8.4 配角深度字段（characters.yaml 新增）

```yaml
supporting_role:
  primary_function: "plot_driver"    # plot_driver | mirror_contrast | world_window | emotion_regulator
  secondary_function: ""             # 可选第二功能
  contrast_type: "positive"          # mirror_contrast专用: positive | negative
  world_aspect: ""                   # world_window专用: 展现的世界观侧面
  signature_tag: ""                  # 最鲜明的1个视觉/语言/行为特征
  extreme_trait: ""                  # 极致化的1-2种性格特质
  independent_goal: ""               # 独立于主角的个人目标（不提主角也能写出）
  self_interest: ""                  # 核心利益诉求
  solo_activity: ""                  # 不跟主角在一起时的典型活动
  lifecycle_stage: introduced        # introduced | deepened | climax | exited
  introduction_chapter: 0
  peak_chapter: 0
  exit_chapter: 0
  exit_type: ""                      # graceful_exit | heroic_death | betrayal | fade_out | transformed
  necessity_check:
    last_check_chapter: 0
    can_remove: false                # Q1
    view_differs: true               # Q2
    has_solo_life: true              # Q3
    action: keep                     # keep | deepen | merge | remove
```

#### 17.8.5 配角生命周期管理

```
引入(introduced) → 深化(deepened) → 高潮(climax) → 退场或转化(exited/transformed)
```

| 阶段 | 章节窗口 | 必须完成 | 禁止 |
|------|---------|---------|------|
| **引入** | 出场后1-3章 | 展示 signature_tag + 暗示 independent_goal | 信息一次性全倒 / 3章后读者仍记不住 |
| **深化** | 引入后3-10章 | 展开独立目标 + 展示观点分歧 + 揭示过往碎片 | 始终围绕主角转 / 性格扁平无变化 |
| **高潮** | 深化后5-10章 | 个人目标与主线交汇，做出关键选择 | 关键时刻缺席 / 选择缺乏铺垫 |
| **退场** | 高潮后1-3章 | 明确退场方式，给读者情感出口 | 「突然消失」/ 反复横跳 |

**配角数量管理**：每卷活跃配角 3-5 个 | 退场一个才能引入下一个 | 主角团核心 ≤ 3 人

**配角质量自检**（每卷至少一次）：
```
□ 每个活跃配角通过必要性三问？
□ 功能类型分布合理（不全是同一类型）？
□ 每个配角 signature_tag 能在3秒内想起？
□ 每个配角 independent_goal 无需提到主角即可表述？
□ 主角团 ≤ 3？活跃配角总数 ≤ 5？
□ 有无幽灵配角（5章+未提但未退场）？
```

> 完整指南：详细示例、反模式大全、创作自检清单 → `references/supporting-character-engine.md`

---

## 模块十八：情感波动曲线引擎（P1）

> 一直爽=疲劳。一直虐=弃书。像音乐和弦进行一样设计：紧张→释放→紧张→释放，但每次天花板抬高。

| 分值 | 情感状态 | 典型事件 |
|------|---------|---------|
| +8~+10 | 极致快感 | 大爽点释放/宿敌覆灭 |
| +3~+7 | 正向推进 | 小胜利/新能力/盟友加入 |
| -3~+3 | 中性过渡 | 日常/铺垫/世界探索 |
| -7~-3 | 压力积累 | 被压制/失败/失去 |
| -10~-8 | 谷底绝望 | 重大损失/信念崩塌 |

**波形规则**：不可连续3章同象限 | 每次谷底反弹高于之前峰 | 前10章振幅小(-5~+7)，后期放开(-10~+10) | 卷高潮必须突破此前情绪天花板。

---

## 模块十九：对话潜台词引擎（P1）

### 19.1 三层信息不对称

| 层 | 内容 | 谁掌握 |
|----|------|--------|
| 表面层 | 嘴上说的字面意思 | 所有在场角色 |
| 意图层 | 说话者真正想表达/达成 | 说话者本人 |
| 潜藏层 | 说话者自己都没意识到的情感 | 只有读者（可能）知道 |

### 19.2 对话自检

- 谁说真话？谁说假话？谁半真半假？
- 谁多知道什么？谁在试探？谁在伪装？
- 有没有一句话——不同角色听出不同意思？

**硬规则**：关键对话禁止「全员坦诚相待」——至少1个角色有隐藏动机。

---

## 模块二十：章节变速引擎（P0）

### 20.1 五种节奏类型

| 类型 | 占比上限 | 特征 | 适用场景 |
|------|---------|------|---------|
| 🚀 冲刺章 | 40% | 高速推进，短句轰炸 | 战斗/追逐/限时危机 |
| 💥 爆发章 | 25% | 大爽点释放，全引擎全开 | 打脸/突破/反转高潮 |
| 🌿 呼吸章 | 20% | 放缓，建立情感/世界沉浸 | 角色互动/场景探索 |
| 🔀 转折章 | 10% | 信息揭示改变一切 | 真相揭露/阵营转换 |
| 🧱 铺垫章 | 5% | 种因播种，缓慢升温 | 大战前夜/新地图导入 |

### 20.2 变速规则

- 呼吸章不可连续（间隔≥3章）/ 每10章最多2章
- 铺垫章其后3章内必须回收（否则=「死铺垫」）
- 冲刺章连续5章后必须插入呼吸章
- 每章开头标注类型：`<!-- 变速: 冲刺章 | D7+D10 -->`

---

## 模块二十一：预加载+智能缓存（P1·效率）

### 21.1 双轨策略

| 类别 | 文件 | 刷新策略 |
|------|------|---------|
| **稳定态** | rules.yaml, world.yaml（已揭示）, 已死角色, 已收束线索 | 全会话缓存，仅版本变更时重读 |
| **动态态** | characters.yaml（存活）, threads.yaml（活跃）, debts, arc-tracker | 每5章或变更时刷新 |

**刷新触发**：刚写完+commit_chapter / 手动patch了状态 / 上次刷新≥5章。

**收益**：减少60%+状态文件读取token消耗。

---

## 模块二十二：批量发散模式（P1·效率）

**适用**：过渡阶段（非高潮/反转/卷末）+ 近3章tension稳定 + 近5章无红黄灯。

**操作**：一次性发散3-5章方向，批量写作，`commit_chapter.py --batch` 批量提交。

**限制**：批量中不得含角色死亡/重大反转/卷终结/格局升级。

---

## 模块二十三：状态文件Delta更新（P2·效率）

| 操作 | 旧方式 | Delta方式 |
|------|--------|----------|
| 角色状态变更 | 全量重写 | 仅patch该角色条目 |
| 新增线索 | 全量重写 | `cat >>` 追加 |
| 时间线事件 | 全量重写 | 仅追加新事件段 |

**每次Delta后必做**：`python3 -c "import yaml; yaml.safe_load(open('file'))"` 验证。解析失败 → 回退全量重写。

---

## 关键陷阱

### 逻辑类
死人复活/物品凭空出现/距离时间矛盾/伏笔烂尾/能力膨胀/角色分裂。→ `references/logic-consistency-taxonomy.md`

### 爽度类
假压制真装逼/天降翻盘/打脸拖沓/爽点单调/旁观者缺失。→ 模块十

### 画面感类
告诉代替画面/全程中景/废画面/微分解过度/感官单一。→ 模块十

### 散文质量类（v4.0新增）
全程视觉+对话零其他感官/句式单调/情绪命名代替展示/角色辨识度低。→ 模块十一

### 结构类（v4.0新增）
章节元素<5/8 → 太薄。密度<3/5 → 太薄。连续3章无配角时刻 → 世界围着主角转。断章类型3连重复 → 读者猜到节奏。→ 模块十二

### 弧线类（v4.0新增）
角色无信念挑战/连续10章情感单调/全员对话坦诚/呼吸章超标。→ 模块十七/十八/十九/二十

### 效率类（v4.0新增）
不变状态反复读取/逐章发散浪费token/全量重写状态文件。→ 模块二十一/二十二/二十三

---

## 快速开始

### 全自动模式（推荐）
1. **初始化**：`python3 scripts/auto_write.py init --name "书名" --mode auto`
2. **创建 GitHub 仓库**（注意顺序）：
   ```bash
   cd ~/novels/书名
   git init
   git config user.email "xxx@xxx.com" && git config user.name "name"
   git add -A && git commit -m "init: 项目初始化"
   git branch -m main
   gh repo create andy-develop/书名 --private --source=. --push
   ```
   如果 `gh repo create` 失败，可手动：创建远程仓库 → `git remote set-url origin https://USER:***@github.com/USER/书名.git` → `git push -u origin main`
3. **提供开头**：告诉 AI "全自动写《书名》，开头如下：[开头内容]"
4. **AI 自动完成**：发散→选择→写作→校验→Git同步，循环直至完成
5. **每章写完**：`commit_chapter.py` 一键更新状态 + `git add -A && git commit -m "ch{NNN}: 标题" && git push origin main`
6. **中途干预**：随时可以说"暂停"/"这一章我来选"/"节奏太慢"
7. **卷间过渡**：`volume_transition.py` 重置状态 + AI 手动完成大纲/角色/线索重写 + git push
8. **字数校准**：`auto_write.py wordcount --dir . --update`（CJK精确统计）
9. **完成**：AI 输出全书摘要、线索收束报告、润色建议 + 最终 git push `[全书完结]`

### 人机协作模式
1. **新建项目**：`python3 scripts/novel_state.py init --name "书名" --dir ~/novels/书名`
2. **创建 GitHub 仓库**：`cd ~/novels/书名 && git init && gh repo create andy-develop/书名 --private --source=. --push`
3. **填写设定**：编辑 state/ 下的 yaml 文件
4. **写每一章前**：运行 `consistency_checker.py check` 查看当前状态
5. **发散+评分**：让 AI 发散剧情（含自动评分），你选方向，AI 写章节
6. **写完每章后**：让 AI 更新状态文件 + git commit & push
7. **每10章**：运行 `consistency_checker.py audit` 做全局审查
